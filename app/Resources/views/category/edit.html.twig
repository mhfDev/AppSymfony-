{% extends 'back.html.twig' %}

{% block content %}
    <h1>Edition Catégorie</h1>

    {{ form_start(edit_form) }}
                <div class="form-group">
            {{ form_row(edit_form.title, { 'attr': {'class':'form-control'}}) }}

        </div>
        <div class="form-group">
            {{ form_row(edit_form.description, { 'attr': {'class':'form-control'}}) }}

        </div>
        <div class="form-group">
            {{ form_row(edit_form.slug, { 'attr': {'class':'form-control'}}) }}

        </div>
        <hr>


        <input class="btn btn-danger" type="submit" value="Editer" />
        <a class="btn btn-default" href="{{ path('admin_category_index') }}">Retour</a>
    {{ form_end(edit_form) }}

    
{% endblock %}
{% block breadcrumb %}
<ol class="breadcrumb">
        <li class="breadcrumb-item"><a href=""> Tableau de bord</a></li>
        <li><a href="breadcrumb-item"> Liste des Catégories</a></li>
        <li class="breadcrumb-item active"> Edition catégorie</li>
    </ol>
    
{% endblock %}

